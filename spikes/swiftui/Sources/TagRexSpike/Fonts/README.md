# Fonts

Filled by `build.sh`: the upstream TTFs of the families the web UI bundles as
woff2 subsets — IBM Plex Sans (OFL) and JetBrains Mono (Apache-2.0). They are
downloaded rather than committed so the repository stays free of binaries.

With this folder empty the stand still builds and runs, on the system faces.
